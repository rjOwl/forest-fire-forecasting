name: C/C++ CI

on:
  push:
    branches: [ "client-prod" ]
  pull_request:
    branches: [ "client-prod" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: ls
    - run: pwd
    - name: make client
      working-directory: forest_fire_forecasting
      run: make client
    - name: client
      working-directory: forest_fire_forecasting
      run: ./client
